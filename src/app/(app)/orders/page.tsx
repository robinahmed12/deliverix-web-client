import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function OrdersPage() {
  return (
    <FeaturePlaceholder
      title="Orders"
      description="Create, track, and manage delivery orders."
      phase="the Orders phase (Phase 2)"
    />
  );
}