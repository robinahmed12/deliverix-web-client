import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function DriversPage() {
  return (
    <FeaturePlaceholder
      title="Drivers"
      description="Manage driver records, vehicles, and availability."
      phase="the Driver & Vehicle phase (Phase 3)"
    />
  );
}