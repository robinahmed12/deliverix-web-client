import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function CustomersPage() {
  return (
    <FeaturePlaceholder
      title="Customers"
      description="Manage customer records and their order history."
      phase="the Customer & Configuration phase (Phase 3)"
    />
  );
}