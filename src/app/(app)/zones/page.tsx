import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function ZonesPage() {
  return (
    <FeaturePlaceholder
      title="Zones"
      description="Manage delivery zones used for pricing and slotting."
      phase="the Customer & Configuration phase (Phase 3)"
    />
  );
}